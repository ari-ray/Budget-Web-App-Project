# Budget Web App - CSS

This folder contains all the CSS files used in the BUdget Web App. The "style.css" file is the main stylesheet for the application, handling layout, colors and UI styling.

---

## Notes
- Files in this folder are referenced in HTML templates via Flask’s url_for('static', filename='...').
- All assets in this folder are static, meaning they are not dynamically generated by the server.
